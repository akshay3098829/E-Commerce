import { Typography } from "@mui/material";
export const MuiTypography =()=>{
    return(
        <div>
        <Typography variant="h1">h1 heading</Typography>
        <Typography variant="h2">h2 heading</Typography>
        <Typography variant="h3">h3 heading</Typography>
        <Typography variant="h4">h4 heading</Typography>
        <Typography variant="subtitle1">Subtitleone</Typography>
        <Typography variant="subtitle2">Subtitletwo</Typography>
        <Typography variant="body1">Note that the development build is not optimized.
To create a production build, use npm run</Typography>
<Typography variant="body2">Note that the development build is not optimized.
To create a production build, use npm run</Typography>
        </div>
    );
}